{if $punto.can_add_osservazioni}
    <p>Le segnalo che il termine ultimo per la presentazione delle osservazioni è attualmente fissato per {attribute_view_gui attribute=$punto.object.data_map.termine_osservazioni}.</p>
{/if}